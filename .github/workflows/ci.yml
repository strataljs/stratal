name: CI
permissions:
  contents: read

on:
  push:
    branches: [main]
    paths:
      - 'packages/**'
      - 'yarn.lock'
      - '.github/workflows/**'
      - '.github/actions/**'
  pull_request:
    branches: [main]
    paths:
      - 'packages/**'
      - 'yarn.lock'
      - '.github/workflows/**'
      - '.github/actions/**'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@58077d3c7e43986b6b15fba718e8ea69e387dfcc # v2.15.1
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: ./.github/actions/setup

      - name: Build all packages
        run: yarn workspaces foreach -At run build

      - name: Verify package contents
        run: yarn workspaces foreach -At --no-private exec npm pack --dry-run

      - name: Upload build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dist-artifacts
          path: packages/*/dist/
          retention-days: 1

  versioning:
    name: Versioning
    runs-on: ubuntu-latest
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@58077d3c7e43986b6b15fba718e8ea69e387dfcc # v2.15.1
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: ./.github/actions/setup

      # Every `@stratal/*` package peers on `stratal`, so changesets decides on
      # its own how hard to bump each of them whenever `stratal` is bumped.
      # Changesets 2.x majored them by default and needed an opt-out; 3.x bumps
      # by patch instead. Either way the decision is made silently, by
      # @changesets/config and @changesets/assemble-release-plan — transitive
      # packages that can move even though @changesets/cli itself is pinned. So
      # don't infer the behaviour from config, assert the plan: catch a
      # surprise major here rather than on npm, where it cannot be taken back.
      #
      # `changeset status` diffs against config.baseBranch (`dev`), which exists
      # only on the Forgejo origin; without it the command fails before printing
      # a plan. Pointing `dev` at HEAD makes that diff a no-op — the release plan
      # is read from `.changeset/`, not from the diff, so the verdict is the same.
      - name: Guard against an unintended major release
        env:
          PLAN: ${{ runner.temp }}/release-plan.json
        run: |
          git rev-parse --verify --quiet dev >/dev/null || git branch dev HEAD
          yarn changeset status --output="$PLAN"

          jq -r '.releases[] | "  \(.name): \(.oldVersion) -> \(.newVersion) (\(.type))"' "$PLAN"

          majors=$(jq -r '[.releases[] | select(.type == "major" and (.oldVersion | startswith("0.")))] | length' "$PLAN")
          if [ "$majors" -gt 0 ]; then
            echo "::error::A major bump is planned while these packages are still below 1.0.0. If that is deliberate, update this guard. Otherwise the peer-dependent escalation is back: check whether @changesets/config or @changesets/assemble-release-plan have moved (they are transitive, so they can move even though @changesets/cli is pinned), and that every peer range on 'stratal' admits the new version. Changesets 3.x bumps peer-dependents by patch; if that default has changed back, this is where it shows up."
            exit 1
          fi

  lint:
    name: Lint & Typecheck
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@58077d3c7e43986b6b15fba718e8ea69e387dfcc # v2.15.1
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: ./.github/actions/setup

      - name: Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: dist-artifacts
          path: packages

      - name: Generate framework schema and types
        run: yarn workspace @stratal/framework pretest

      - name: Lint
        run: yarn lint

      - name: Typecheck
        run: yarn workspaces foreach -At --no-private run typecheck

  test:
    name: Test
    needs: build
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: stratal_test
          POSTGRES_USER: stratal
          POSTGRES_PASSWORD: stratal_test
        ports:
          - 5438:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://stratal:stratal_test@localhost:5438/stratal_test
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@58077d3c7e43986b6b15fba718e8ea69e387dfcc # v2.15.1
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: ./.github/actions/setup

      - name: Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: dist-artifacts
          path: packages

      - name: Create framework .env for database
        run: echo "DATABASE_URL=$DATABASE_URL" > packages/framework/.env

      - name: Generate framework schema and types
        run: yarn workspace @stratal/framework pretest

      - name: Run tests
        run: yarn workspaces foreach -At --no-private run test
