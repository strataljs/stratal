---
"@stratal/testing": patch
---

Add test utilities for Quarry command framework

### Details

- Add `TestCommandRequest` builder for constructing command inputs in tests
- Add `TestCommandResult` for asserting command output, exit codes, and errors
- Export new utilities from `@stratal/testing` main entry point
